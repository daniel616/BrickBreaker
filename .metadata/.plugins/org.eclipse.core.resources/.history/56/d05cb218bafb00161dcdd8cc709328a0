package commands.TurtleCommands;

import java.util.List;

import application.Turtle;
import application.TurtleState;
import exceptions.StateNotFoundException;

/** PenUp makes the Turtle object put its pen up so that it now may not draw when it moves. It returns 0.
 * 
 * @author Kyle
 *
 */
public class PenUp extends TurtleCommands {
	
	public static final int VARIABLES = 0;

	public PenUp(List<Double> args) {
		super(args);
	}

	@Override
	public double execute(Turtle t) throws StateNotFoundException {
		TurtleState lastState = t.getFinalTurtleState();
		t.addTurtleState(lastState.getX(), lastState.getY(), lastState.getHeading(), false,
				lastState.getColor(), lastState.turtleShowing());
		return 0;
	}

}
