package commands.TurtleCommands;

import java.util.List;

import TurtleDetails.Turtle;
import TurtleDetails.TurtleState;
import exceptions.CommandException;
import exceptions.StateNotFoundException;

/** Right turns the Turtle's heading right by the entered degrees and returns degrees turned.
 * 
 * @author Kyle
 *
 */
public class Right extends TurtleCommands {
	
	public static final int VARIABLES = 1;

	public Right(List<String> args) throws CommandException {
		super(args);
		if (args.size() != VARIABLES) {
			throw new RuntimeException("Incorrect number of variables in entered list");
		}
	}

	@Override
	public double execute(Turtle t) throws StateNotFoundException {
		TurtleState lastState = t.getFinalTurtleState();
		
		double normalizedHeading = lastState.getHeading()-doubleArguments.get(0);
		
		t.addTurtleState(lastState.getX(), lastState.getY(), normalizedHeading, lastState.penDown(), lastState.getColor(), lastState.turtleShowing());
		return doubleArguments.get(0);
	}

}
